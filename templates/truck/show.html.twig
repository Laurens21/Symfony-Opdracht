{% extends 'base.html.twig' %}

{% block title %}Truck{% endblock %}

{% block body %}
    <h1>Truck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ truck.id }}</td>
            </tr>
            <tr>
                <th>Kenteken</th>
                <td>{{ truck.kenteken }}</td>
            </tr>
            <tr>
                <th>Merk</th>
                <td>{{ truck.merk }}</td>
            </tr>
            <tr>
                <th>Bouwjaar</th>
                <td>{{ truck.bouwjaar ? truck.bouwjaar|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a style="margin: 10px" class="btn btn-dark" href="{{ path('truck_index') }}">back to list</a>
    <div>
    <a style="margin: 10px" class="btn btn-success" href="{{ path('truck_edit', {'id': truck.id}) }}">edit</a>
    </div>
    {{ include('truck/_delete_form.html.twig') }}
{% endblock %}
